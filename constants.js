const ONE_SECOND = 1000;
const ONE_MINUTE = ONE_SECOND * 60;
const ONE_HOUR = ONE_MINUTE * 60;
const ONE_DAY = ONE_HOUR * 24;

const MANUAL_TEXT = `MANUAL TEXT HERE....`

module.exports = {
    ONE_DAY,
    MANUAL_TEXT
};
